// Auto-generated. Do not edit!

// (in-package cartesian_planner.msg)


"use strict";

const _serializer = _ros_msg_utils.Serialize;
const _arraySerializer = _serializer.Array;
const _deserializer = _ros_msg_utils.Deserialize;
const _arrayDeserializer = _deserializer.Array;
const _finder = _ros_msg_utils.Find;
const _getByteLength = _ros_msg_utils.getByteLength;
let geometry_msgs = _finder('geometry_msgs');

//-----------------------------------------------------------

class cart_moveGoal {
  constructor(initObj={}) {
    if (initObj === null) {
      // initObj === null is a special case for deserialization where we don't initialize fields
      this.command_code = null;
      this.des_pose_gripper = null;
      this.arm_dp = null;
      this.q_goal = null;
      this.time_scale_stretch_factor = null;
    }
    else {
      if (initObj.hasOwnProperty('command_code')) {
        this.command_code = initObj.command_code
      }
      else {
        this.command_code = 0;
      }
      if (initObj.hasOwnProperty('des_pose_gripper')) {
        this.des_pose_gripper = initObj.des_pose_gripper
      }
      else {
        this.des_pose_gripper = new geometry_msgs.msg.PoseStamped();
      }
      if (initObj.hasOwnProperty('arm_dp')) {
        this.arm_dp = initObj.arm_dp
      }
      else {
        this.arm_dp = [];
      }
      if (initObj.hasOwnProperty('q_goal')) {
        this.q_goal = initObj.q_goal
      }
      else {
        this.q_goal = [];
      }
      if (initObj.hasOwnProperty('time_scale_stretch_factor')) {
        this.time_scale_stretch_factor = initObj.time_scale_stretch_factor
      }
      else {
        this.time_scale_stretch_factor = 0.0;
      }
    }
  }

  static serialize(obj, buffer, bufferOffset) {
    // Serializes a message object of type cart_moveGoal
    // Serialize message field [command_code]
    bufferOffset = _serializer.int32(obj.command_code, buffer, bufferOffset);
    // Serialize message field [des_pose_gripper]
    bufferOffset = geometry_msgs.msg.PoseStamped.serialize(obj.des_pose_gripper, buffer, bufferOffset);
    // Serialize message field [arm_dp]
    bufferOffset = _arraySerializer.float64(obj.arm_dp, buffer, bufferOffset, null);
    // Serialize message field [q_goal]
    bufferOffset = _arraySerializer.float64(obj.q_goal, buffer, bufferOffset, null);
    // Serialize message field [time_scale_stretch_factor]
    bufferOffset = _serializer.float64(obj.time_scale_stretch_factor, buffer, bufferOffset);
    return bufferOffset;
  }

  static deserialize(buffer, bufferOffset=[0]) {
    //deserializes a message object of type cart_moveGoal
    let len;
    let data = new cart_moveGoal(null);
    // Deserialize message field [command_code]
    data.command_code = _deserializer.int32(buffer, bufferOffset);
    // Deserialize message field [des_pose_gripper]
    data.des_pose_gripper = geometry_msgs.msg.PoseStamped.deserialize(buffer, bufferOffset);
    // Deserialize message field [arm_dp]
    data.arm_dp = _arrayDeserializer.float64(buffer, bufferOffset, null)
    // Deserialize message field [q_goal]
    data.q_goal = _arrayDeserializer.float64(buffer, bufferOffset, null)
    // Deserialize message field [time_scale_stretch_factor]
    data.time_scale_stretch_factor = _deserializer.float64(buffer, bufferOffset);
    return data;
  }

  static getMessageSize(object) {
    let length = 0;
    length += geometry_msgs.msg.PoseStamped.getMessageSize(object.des_pose_gripper);
    length += 8 * object.arm_dp.length;
    length += 8 * object.q_goal.length;
    return length + 20;
  }

  static datatype() {
    // Returns string type for a message object
    return 'cartesian_planner/cart_moveGoal';
  }

  static md5sum() {
    //Returns md5sum for a message object
    return '5bd816596081b2b0fbcdf7dad29bf944';
  }

  static messageDefinition() {
    // Returns full string definition for message
    return `
    # ====== DO NOT MODIFY! AUTOGENERATED FROM AN ACTION DEFINITION ======
    #cartesian-move action interface
    #minimally, it may contain just a command code
    #more generally, it may contain desired tool-frame pose, as well
    # as gripper pose (gripper opening, or vacuum gripper on/off)
    # and an arrival time for the move
    # It is assumed that a move starts from the previous commanded pose, or from the current joint state
    
    #return codes provide status info, e.g. if a proposed move is reachable
    
    #define message constants:  
    uint8 ARM_TEST_MODE = 0
    
    #queries
    uint8 ARM_IS_SERVER_BUSY_QUERY = 1
    uint8 ARM_QUERY_IS_PATH_VALID = 2
    uint8 GET_TOOL_POSE = 5
    uint8 GET_Q_DATA = 7
    
    #requests for motion plans; 
    uint8 PLAN_PATH_CURRENT_TO_WAITING_POSE=20
    #uint8 PLAN_PATH_CURRENT_TO_PRE_POSE=20 #synonym
    uint8 PLAN_JSPACE_PATH_CURRENT_TO_CART_GRIPPER_POSE = 21 #plan a joint-space path from current arm pose to some IK soln of Cartesian goal
    
    uint8 PLAN_PATH_CURRENT_TO_GOAL_GRIPPER_POSE=22 #plan cartesian path from current arm pose to goal gripper pose
    uint8 PLAN_FINE_PATH_CURRENT_TO_GOAL_GRIPPER_POSE = 23 #plan path to specified gripper pose #as above, but hi-res
    uint8 PLAN_PATH_CURRENT_TO_GOAL_DP_XYZ = 24 #rectilinear translation w/ fixed orientation
    uint8 PLAN_JSPACE_PATH_CURRENT_TO_QGOAL = 25
     
    
    uint8 TIME_RESCALE_PLANNED_TRAJECTORY = 40 #can make arm go slower/faster with provided time-stretch factor
    uint8 REFINE_PLANNED_TRAJECTORY = 41 #if used approx IK soln, use this option to refine solns
    uint8 SET_ARRIVAL_TIME_PLANNED_TRAJECTORY = 42 #used to set desired arrival time; put arrival time value in goal time_scale_stretch_factor
    
    # request to preview plan:
    #uint8 DISPLAY_TRAJECTORY = 50
    
    #MOVE command!
    uint8 EXECUTE_PLANNED_PATH = 100
    
    #uint8 ARM_DESCEND_20CM=101
    #uint8 ARM_DEPART_20CM=102
    
    
    #goal:
    int32 command_code
    geometry_msgs/PoseStamped des_pose_gripper
    float64[] arm_dp #to command a 3-D vector displacement relative to current pose, fixed orientation
    float64[] q_goal
    float64 time_scale_stretch_factor
    
    ================================================================================
    MSG: geometry_msgs/PoseStamped
    # A Pose with reference coordinate frame and timestamp
    Header header
    Pose pose
    
    ================================================================================
    MSG: std_msgs/Header
    # Standard metadata for higher-level stamped data types.
    # This is generally used to communicate timestamped data 
    # in a particular coordinate frame.
    # 
    # sequence ID: consecutively increasing ID 
    uint32 seq
    #Two-integer timestamp that is expressed as:
    # * stamp.sec: seconds (stamp_secs) since epoch (in Python the variable is called 'secs')
    # * stamp.nsec: nanoseconds since stamp_secs (in Python the variable is called 'nsecs')
    # time-handling sugar is provided by the client library
    time stamp
    #Frame this data is associated with
    string frame_id
    
    ================================================================================
    MSG: geometry_msgs/Pose
    # A representation of pose in free space, composed of position and orientation. 
    Point position
    Quaternion orientation
    
    ================================================================================
    MSG: geometry_msgs/Point
    # This contains the position of a point in free space
    float64 x
    float64 y
    float64 z
    
    ================================================================================
    MSG: geometry_msgs/Quaternion
    # This represents an orientation in free space in quaternion form.
    
    float64 x
    float64 y
    float64 z
    float64 w
    
    `;
  }

  static Resolve(msg) {
    // deep-construct a valid message object instance of whatever was passed in
    if (typeof msg !== 'object' || msg === null) {
      msg = {};
    }
    const resolved = new cart_moveGoal(null);
    if (msg.command_code !== undefined) {
      resolved.command_code = msg.command_code;
    }
    else {
      resolved.command_code = 0
    }

    if (msg.des_pose_gripper !== undefined) {
      resolved.des_pose_gripper = geometry_msgs.msg.PoseStamped.Resolve(msg.des_pose_gripper)
    }
    else {
      resolved.des_pose_gripper = new geometry_msgs.msg.PoseStamped()
    }

    if (msg.arm_dp !== undefined) {
      resolved.arm_dp = msg.arm_dp;
    }
    else {
      resolved.arm_dp = []
    }

    if (msg.q_goal !== undefined) {
      resolved.q_goal = msg.q_goal;
    }
    else {
      resolved.q_goal = []
    }

    if (msg.time_scale_stretch_factor !== undefined) {
      resolved.time_scale_stretch_factor = msg.time_scale_stretch_factor;
    }
    else {
      resolved.time_scale_stretch_factor = 0.0
    }

    return resolved;
    }
};

// Constants for message
cart_moveGoal.Constants = {
  ARM_TEST_MODE: 0,
  ARM_IS_SERVER_BUSY_QUERY: 1,
  ARM_QUERY_IS_PATH_VALID: 2,
  GET_TOOL_POSE: 5,
  GET_Q_DATA: 7,
  PLAN_PATH_CURRENT_TO_WAITING_POSE: 20,
  PLAN_JSPACE_PATH_CURRENT_TO_CART_GRIPPER_POSE: 21,
  PLAN_PATH_CURRENT_TO_GOAL_GRIPPER_POSE: 22,
  PLAN_FINE_PATH_CURRENT_TO_GOAL_GRIPPER_POSE: 23,
  PLAN_PATH_CURRENT_TO_GOAL_DP_XYZ: 24,
  PLAN_JSPACE_PATH_CURRENT_TO_QGOAL: 25,
  TIME_RESCALE_PLANNED_TRAJECTORY: 40,
  REFINE_PLANNED_TRAJECTORY: 41,
  SET_ARRIVAL_TIME_PLANNED_TRAJECTORY: 42,
  EXECUTE_PLANNED_PATH: 100,
}

module.exports = cart_moveGoal;
